```python ### FILE: app/exceptions.py ```python """Custom exception hierarchy for MCP Email Service domain errors. All domain-specific exceptions inherit from :class:`MCPEmailError`, making it easy to catch any application error broadly or to target a specific sub-category. Each exception class carries contextual information (e.g. ``account_id``) where appropriate so that error handlers and API middleware can produce meaningful responses. """ from typing import Optional class MCPEmailError(Exception): """Base exception for MCP email service. All custom exceptions in this project should inherit from this class so that callers can catch the entire family with a single ``except MCPEmailError`` clause. Attributes: message: Human-readable description of the error. detail: Optional extra context or machine-readable detail. """ def __init__(self, message: str = "", detail: Optional[str] = None) -> None: self.message = message self.detail = detail super().__init__(message) def __str__(self) -> str: if self.detail: return f"{self.message} — {self.detail}" return self.message class AccountNotFoundError(MCPEmailError): """Raised when account_id not found. Carries the ``account_id`` that could not be resolved so that callers and error handlers can reference it directly. Attributes: account_id: The identifier that was looked up without success. """ def __init__(self, account_id: str, detail: Optional[str] = None) -> None: self.account_id = account_id message = f"Account not found: {account_id}" super().__init__(message=message, detail=detail) class SyncInProgressError(MCPEmailError): """Raised when sync already in progress for account. Indicates that a sync operation is already running for the given account and a second concurrent attempt was rejected. Attributes: account_id: The identifier of the account that already has an active sync. """ def __init__(self, account_id: str, detail: Optional[str] = None) -> None: self.account_id = account_id message = f"Sync already in progress for account: {account_id}" super().__init__(message=message, detail=detail) class IMAPConnectionError(MCPEmailError): """Raised when IMAP connection fails. Wraps lower-level IMAP / socket errors into a domain-level exception so that callers don't need to handle transport-specific exceptions. Attributes: host: The IMAP server host that was being contacted, if available. """ def __init__( self, message: str = "IMAP connection failed", host: Optional[str] = None, detail: Optional[str] = None, ) -> None: self.host = host super().__init__(message=message, detail=detail) class CredentialValidationError(MCPEmailError): """Raised when IMAP credentials are invalid. Typically emitted during an account's credential verification step (e.g. LOGIN command rejected by the IMAP server). Attributes: account_id: The identifier of the account whose credentials were rejected, if known. """ def __init__( self, message: str = "Invalid IMAP credentials", account_id: Optional[str] = None, detail: Optional[str] = None, ) -> None: self.account_id = account_id super().__init__(message=message, detail=detail) ```